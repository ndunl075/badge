{
  "name": "@badge/middleware",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Composes the verifier, the policy engine and the sinks into one decision.",
  "license": "Apache-2.0",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "dependencies": {
    "@badge/core": "workspace:*",
    "@badge/directory": "workspace:*",
    "@badge/policy": "workspace:*"
  },
  "devDependencies": {
    "@badge/testkit": "workspace:*"
  }
}
